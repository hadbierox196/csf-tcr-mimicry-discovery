=========================================
CSF TCR-pMHC Molecular Mimicry Discovery
=========================================

This report was generated by ``snakemake --report``. It documents the
exact DAG, rule runtimes, and per-file provenance for one pipeline run.

Status note: if this run used ``--config smoke_test=true``, the
structural-confidence values in ``risk_scores.json`` are a fixed
placeholder, not real structure-prediction output -- see
``workflow/scripts/structure_prediction.py`` and ``docs/model_card.md``.

See ``docs/roadmap.md`` for what's implemented vs. still scaffolded.
